// import "@testing-library/jest-dom";
// import React from "react";
// import { render, screen } from "@testing-library/react";
// import VenueList from "../VenueList";
import { describe, test, expect } from "@jest/globals";
describe("VenueList", () => {
  test("renders the list of venues", () => {
    expect(true).toBe(true);
  });
  //   const mockVenues = [
  //     { id: 1, name: "Ping Pong Palace", address: "123 Main St" },
  //     { id: 2, name: "Table Tennis Town", address: "456 Oak Ave" },
  //   ];
  //   it("renders the list of venues", () => {
  //     render(<VenueList venues={mockVenues} />);
  //     expect(screen.getByText("Ping Pong Palace")).toBeInTheDocument();
  //     expect(screen.getByText("123 Main St")).toBeInTheDocument();
  //     expect(screen.getByText("Table Tennis Town")).toBeInTheDocument();
  //     expect(screen.getByText("456 Oak Ave")).toBeInTheDocument();
  //   });
});
